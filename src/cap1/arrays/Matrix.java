package cap1.arrays;

public class Matrix {
	// Step 1. Create attributes
	private int size;
	private int[] elementsFilas;
	private int[] elementsColumnas;
	
	// Step 1.1. Create Constructor
	public Matrix(int newSize) {
		System.out.println("Creating matrix with size=" + newSize);
		this.size = newSize;
		this.elementsFilas = new int[newSize];
		this.elementsColumnas = new int[newSize];
		
	// Step 2. Create methods
		
	// Step 2.1. Generate encapsulation methods
	// Step 2.2. Create CRUD methods
}
